# gui applications that install pkgs (so password is required)

# update and upgrade all packages and cleanup old versions
update
upgrade
cleanup

# managed by homebrew-cask
install phinze/cask/brew-cask
cask cleanup

# development tools
cask install xquartz
cask install parallels
cask install virtualbox
cask install crosspack-avr

# because without netflix I am nothing
cask install silverlight

# cleanup afterwards
cask cleanup